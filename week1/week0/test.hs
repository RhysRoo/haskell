square :: Int -> Int
square n = n * n

twiceSum :: Int -> Int -> Int
twiceSum x y = 2 * (x + y)

divideNum :: Float -> Float -> Float
divideNum x y = x / y

smallsum :: Float -> Float -> Float
smallsum x y = (x * y) / 4